<Window x:Class="ExpressPackingMonitoring.UI.FirstUseSetupWizardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="电脑录像配置向导"
        Height="690"
        Width="920"
        MinHeight="660"
        MinWidth="860"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Icon="/app.ico"
        Background="{DynamicResource WindowBackground}"
        FontFamily="Microsoft YaHei UI, Segoe UI">
    <Window.Resources>
        <Style x:Key="WizardCardButtonStyle" TargetType="RadioButton">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="Root"
                                Padding="16"
                                Background="{DynamicResource PanelBackground}"
                                BorderBrush="{DynamicResource BorderDefault}"
                                BorderThickness="1"
                                CornerRadius="8">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="22"/>
                                    <ColumnDefinition Width="14"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid Width="18"
                                      Height="18"
                                      HorizontalAlignment="Center"
                                      VerticalAlignment="Center">
                                    <Ellipse x:Name="Outer"
                                             Stroke="{DynamicResource BorderStrong}"
                                             StrokeThickness="2"/>
                                    <Ellipse x:Name="Inner"
                                             Width="8"
                                             Height="8"
                                             Fill="{DynamicResource AccentBlue}"
                                             HorizontalAlignment="Center"
                                             VerticalAlignment="Center"
                                             Opacity="0"/>
                                </Grid>
                                <ContentPresenter Grid.Column="2"
                                                  VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="BorderBrush" Value="{DynamicResource AccentBlue}"/>
                                <Setter TargetName="Root" Property="Background" Value="{DynamicResource BorderLight}"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Root" Property="BorderBrush" Value="{DynamicResource AccentBlue}"/>
                                <Setter TargetName="Outer" Property="Stroke" Value="{DynamicResource AccentBlue}"/>
                                <Setter TargetName="Inner" Property="Opacity" Value="1"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="StepTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{DynamicResource TextSecondary}"/>
            <Setter Property="Margin" Value="0,0,0,14"/>
        </Style>
    </Window.Resources>

    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,22">
            <TextBlock Text="电脑录像配置向导"
                       FontSize="28"
                       FontWeight="Black"
                       Foreground="{DynamicResource TextPrimary}"/>
            <TextBlock Text="按步骤配置摄像头、面单识别方式、麦克风和可选扫码枪"
                       Margin="0,8,0,0"
                       FontSize="14"
                       Foreground="{DynamicResource TextSecondary}"/>
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="190"/>
                <ColumnDefinition Width="24"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0"
                    Background="{DynamicResource ControlBackground}"
                    BorderBrush="{DynamicResource BorderLight}"
                    BorderThickness="1"
                    CornerRadius="12"
                    Padding="18">
                <StackPanel>
                    <TextBlock x:Name="StepModeText" Text="1. 选择打包模式" Style="{StaticResource StepTextStyle}"/>
                    <TextBlock x:Name="StepCameraText" Text="2. 选择摄像头" Style="{StaticResource StepTextStyle}"/>
                    <TextBlock x:Name="StepCameraRecognitionText" Text="3. 摄像头识别" Style="{StaticResource StepTextStyle}"/>
                    <TextBlock x:Name="StepRecordingProfileText" Text="4. 录制性能检测" Style="{StaticResource StepTextStyle}"/>
                    <TextBlock x:Name="StepMicText" Text="5. 选择麦克风" Style="{StaticResource StepTextStyle}"/>
                    <TextBlock x:Name="StepScannerText" Text="6. 扫码枪（可选）" Style="{StaticResource StepTextStyle}"/>
                    <TextBlock x:Name="StepDoneText" Text="7. 完成" Style="{StaticResource StepTextStyle}"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="2"
                    Background="{DynamicResource PanelBackground}"
                    BorderBrush="{DynamicResource BorderLight}"
                    BorderThickness="1"
                    CornerRadius="12"
                    Padding="24">
                <Grid>
                    <Grid x:Name="ModePage">
                        <StackPanel>
                            <TextBlock Text="选择打包模式"
                                       FontSize="22"
                                       FontWeight="Black"
                                       Foreground="{DynamicResource TextPrimary}"/>
                            <TextBlock Text="推荐连续打包模式，识别或扫描下一张面单时自动保存上一单。"
                                       Margin="0,8,0,22"
                                       FontSize="14"
                                       Foreground="{DynamicResource TextSecondary}"/>
                            <RadioButton x:Name="ContinuousModeRadio"
                                         GroupName="PackingMode"
                                         IsChecked="True"
                                         Style="{StaticResource WizardCardButtonStyle}">
                                <StackPanel>
                                    <DockPanel LastChildFill="False">
                                        <TextBlock Text="连续打包模式"
                                                   FontSize="18"
                                                   FontWeight="Black"
                                                   Foreground="{DynamicResource TextPrimary}"/>
                                        <Border Margin="10,0,0,0"
                                                Padding="8,3"
                                                Background="{DynamicResource AccentBlue}"
                                                CornerRadius="6">
                                            <TextBlock Text="推荐" Foreground="White" FontSize="12" FontWeight="Bold"/>
                                        </Border>
                                    </DockPanel>
                                    <TextBlock Text="识别或扫描下一张面单条码后，自动结束上一单并开始下一单。"
                                               Margin="0,7,0,0"
                                               FontSize="14"
                                               TextWrapping="Wrap"
                                               Foreground="{DynamicResource TextSecondary}"/>
                                </StackPanel>
                            </RadioButton>
                            <RadioButton x:Name="SameCodeModeRadio"
                                         GroupName="PackingMode"
                                         Style="{StaticResource WizardCardButtonStyle}">
                                <StackPanel>
                                    <TextBlock Text="同码停录模式"
                                               FontSize="18"
                                               FontWeight="Black"
                                               Foreground="{DynamicResource TextPrimary}"/>
                                    <TextBlock Text="识别或扫描面单条码开始录制，再次识别同一条码停止录制。"
                                               Margin="0,7,0,0"
                                               FontSize="14"
                                               TextWrapping="Wrap"
                                               Foreground="{DynamicResource TextSecondary}"/>
                                </StackPanel>
                            </RadioButton>
                        </StackPanel>
                    </Grid>

                    <Grid x:Name="CameraPage" Visibility="Collapsed">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <StackPanel>
                            <TextBlock Text="选择摄像头"
                                       FontSize="22"
                                       FontWeight="Black"
                                       Foreground="{DynamicResource TextPrimary}"/>
                            <TextBlock Text="选择录像使用的摄像头，并确认实时画面显示正常"
                                       Margin="0,8,0,20"
                                       FontSize="14"
                                       Foreground="{DynamicResource TextSecondary}"/>
                        </StackPanel>
                        <ComboBox x:Name="CameraComboBox"
                                  Grid.Row="1"
                                  Height="38"
                                  DisplayMemberPath="Name"
                                  SelectionChanged="CameraComboBox_SelectionChanged"
                                  Margin="0,0,0,16"/>
                        <Border Grid.Row="2"
                                Background="Black"
                                BorderBrush="{DynamicResource BorderDefault}"
                                BorderThickness="1"
                                CornerRadius="8"
                                ClipToBounds="True">
                            <Grid>
                                <Image x:Name="CameraPreviewImage"
                                       Stretch="Uniform"/>
                                <Button x:Name="RotateCameraButton"
                                        HorizontalAlignment="Right"
                                        VerticalAlignment="Center"
                                        Margin="0,0,14,0"
                                        Padding="12,8"
                                        Click="RotateCameraButton_Click"
                                        Style="{StaticResource SecondarySmallButtonStyle}"
                                        AutomationProperties.Name="旋转摄像头画面 180 度">
                                    <StackPanel Orientation="Horizontal">
                                        <Path Data="{StaticResource FluentArrowSyncIcon}"
                                              Width="15" Height="15" Stretch="Uniform"
                                              Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                                              Margin="0,0,6,0"/>
                                        <TextBlock x:Name="RotateCameraButtonText" Text="旋转 180°"/>
                                    </StackPanel>
                                </Button>
                                <TextBlock x:Name="CameraStatusText"
                                           Text="正在准备摄像头预览..."
                                           Foreground="#DDFFFFFF"
                                           Background="#66000000"
                                           Padding="10,6"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"
                                           TextWrapping="Wrap"/>
                            </Grid>
                        </Border>
                    </Grid>

                    <Grid x:Name="CameraRecognitionPage" Visibility="Collapsed">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <StackPanel>
                            <TextBlock Text="是否使用摄像头识别面单"
                                       FontSize="22"
                                       FontWeight="Black"
                                       Foreground="{DynamicResource TextPrimary}"/>
                            <TextBlock Text="开启后可直接将面单条形码放入画面，识别成功后自动开始或切换录像"
                                       Margin="0,8,0,16"
                                       FontSize="14"
                                       TextWrapping="Wrap"
                                       Foreground="{DynamicResource TextSecondary}"/>
                        </StackPanel>

                        <Grid Grid.Row="1" Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="12"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <RadioButton x:Name="UseCameraRecognitionRadio"
                                         GroupName="CameraRecognitionChoice"
                                         Margin="0"
                                         Checked="CameraRecognitionChoice_Changed"
                                         Style="{StaticResource WizardCardButtonStyle}">
                                <StackPanel>
                                    <DockPanel LastChildFill="False">
                                        <TextBlock Text="使用摄像头识别"
                                                   FontSize="16"
                                                   FontWeight="Bold"
                                                   Foreground="{DynamicResource TextPrimary}"/>
                                        <Border Margin="8,0,0,0"
                                                Padding="7,2"
                                                Background="{DynamicResource AccentBlue}"
                                                CornerRadius="5">
                                            <TextBlock Text="推荐"
                                                       Foreground="White"
                                                       FontSize="11"
                                                       FontWeight="Bold"/>
                                        </Border>
                                    </DockPanel>
                                    <TextBlock Text="实时测试画面中的面单条码"
                                               Margin="0,5,0,0"
                                               FontSize="13"
                                               Foreground="{DynamicResource TextSecondary}"/>
                                </StackPanel>
                            </RadioButton>
                            <RadioButton x:Name="DisableCameraRecognitionRadio"
                                         Grid.Column="2"
                                         GroupName="CameraRecognitionChoice"
                                         Margin="0"
                                         Checked="CameraRecognitionChoice_Changed"
                                         Style="{StaticResource WizardCardButtonStyle}">
                                <StackPanel>
                                    <TextBlock Text="不使用摄像头识别"
                                               FontSize="16"
                                               FontWeight="Bold"
                                               Foreground="{DynamicResource TextPrimary}"/>
                                    <TextBlock Text="改用扫码枪或手动输入面单号"
                                               Margin="0,5,0,0"
                                               FontSize="13"
                                               Foreground="{DynamicResource TextSecondary}"/>
                                </StackPanel>
                            </RadioButton>
                        </Grid>

                        <Grid Grid.Row="2">
                            <Border x:Name="CameraRecognitionPreviewPanel"
                                    Background="Black"
                                    BorderBrush="{DynamicResource BorderDefault}"
                                    BorderThickness="1"
                                    CornerRadius="8"
                                    ClipToBounds="True">
                                <Grid>
                                    <Image x:Name="CameraRecognitionPreviewImage"
                                           Stretch="Uniform"/>
                                    <Grid x:Name="CameraRecognitionGuide"
                                          Visibility="Collapsed"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          IsHitTestVisible="False">
                                        <Rectangle x:Name="CameraRecognitionGuideBorder"
                                                   Stroke="#DD0078D7"
                                                   StrokeThickness="3"
                                                   StrokeDashArray="6 3"
                                                   Fill="#080078D7"
                                                   RadiusX="6"
                                                   RadiusY="6"/>
                                        <Border x:Name="CameraRecognitionStatusBorder"
                                                HorizontalAlignment="Center"
                                                VerticalAlignment="Top"
                                                Margin="0,10,0,0"
                                                Padding="12,6"
                                                Background="#DD0067B8"
                                                CornerRadius="6">
                                            <TextBlock x:Name="CameraRecognitionStatusText"
                                                       Text="将面单条形码放入框内"
                                                       Foreground="White"
                                                       FontSize="13"
                                                       FontWeight="SemiBold"/>
                                        </Border>
                                    </Grid>
                                    <TextBlock x:Name="CameraRecognitionPreviewStatusText"
                                               Text="正在准备摄像头识别预览..."
                                               Foreground="#DDFFFFFF"
                                               Background="#66000000"
                                               Padding="10,6"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"
                                               TextWrapping="Wrap"/>
                                    <TextBlock x:Name="CameraRecognitionSelectedCameraText"
                                               Margin="10"
                                               Padding="8,4"
                                               HorizontalAlignment="Left"
                                               VerticalAlignment="Bottom"
                                               Foreground="#DDFFFFFF"
                                               Background="#66000000"
                                               FontSize="12"
                                               TextTrimming="CharacterEllipsis"/>
                                </Grid>
                            </Border>
                            <Border x:Name="CameraRecognitionDisabledPanel"
                                    Visibility="Collapsed"
                                    Background="{DynamicResource ControlBackground}"
                                    BorderBrush="{DynamicResource BorderDefault}"
                                    BorderThickness="1"
                                    CornerRadius="8"
                                    Padding="24">
                                <StackPanel HorizontalAlignment="Center"
                                            VerticalAlignment="Center">
                                    <TextBlock Text="已选择不使用摄像头识别"
                                               HorizontalAlignment="Center"
                                               FontSize="17"
                                               FontWeight="Bold"
                                               Foreground="{DynamicResource TextPrimary}"/>
                                    <TextBlock Text="仍可使用扫码枪或在主页面手动输入面单号"
                                               Margin="0,8,0,0"
                                               HorizontalAlignment="Center"
                                               FontSize="14"
                                               Foreground="{DynamicResource TextSecondary}"/>
                                </StackPanel>
                            </Border>
                        </Grid>
                    </Grid>

                    <Grid x:Name="RecordingProfilePage" Visibility="Collapsed">
                        <StackPanel>
                            <TextBlock Text="检测录制性能"
                                       FontSize="22"
                                       FontWeight="Black"
                                       Foreground="{DynamicResource TextPrimary}"/>
                            <TextBlock Text="正在根据这台电脑的编码能力，为已选摄像头推荐稳定的原生分辨率和帧率"
                                       Margin="0,8,0,22"
                                       FontSize="14"
                                       TextWrapping="Wrap"
                                       Foreground="{DynamicResource TextSecondary}"/>

                            <Border Background="{DynamicResource ControlBackground}"
                                    BorderBrush="{DynamicResource BorderDefault}"
                                    BorderThickness="1"
                                    CornerRadius="8"
                                    Padding="20">
                                <StackPanel>
                                    <TextBlock x:Name="RecordingProfileStatusText"
                                               Text="正在测试 720P、1080P、2K 和 4K 的实时编码能力，请稍候"
                                               FontSize="16"
                                               FontWeight="SemiBold"
                                               TextWrapping="Wrap"
                                               Foreground="{DynamicResource AccentBlue}"/>
                                    <ProgressBar x:Name="RecordingProfileProgress"
                                                 Height="6"
                                                 Margin="0,18,0,0"
                                                 IsIndeterminate="True"
                                                 Foreground="{DynamicResource AccentBlue}"/>

                                    <Border x:Name="RecordingProfileResultPanel"
                                            Margin="0,20,0,0"
                                            Padding="16"
                                            Visibility="Collapsed"
                                            Background="{DynamicResource PanelBackground}"
                                            BorderBrush="{DynamicResource BorderLight}"
                                            BorderThickness="1"
                                            CornerRadius="8">
                                        <StackPanel>
                                            <TextBlock x:Name="RecordingProfileResultTitle"
                                                       Text="已自动选择录制配置"
                                                       FontSize="15"
                                                       FontWeight="Bold"
                                                       Foreground="{DynamicResource AccentGreen}"/>
                                            <TextBlock x:Name="RecordingProfileResultText"
                                                       Margin="0,10,0,0"
                                                       FontSize="15"
                                                       LineHeight="24"
                                                       TextWrapping="Wrap"
                                                       Foreground="{DynamicResource TextPrimary}"/>
                                            <TextBlock Text="点击“下一步”继续测试麦克风；完成整个向导后配置才会保存"
                                                       Margin="0,12,0,0"
                                                       FontSize="13"
                                                       TextWrapping="Wrap"
                                                       Foreground="{DynamicResource TextSecondary}"/>
                                            <Button x:Name="RecordingProfileRetryButton"
                                                    Content="重新检测"
                                                    Margin="0,14,0,0"
                                                    Padding="16,7"
                                                    HorizontalAlignment="Left"
                                                    Visibility="Collapsed"
                                                    Click="RecordingProfileRetryButton_Click"/>
                                        </StackPanel>
                                    </Border>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </Grid>

                    <Grid x:Name="MicPage" Visibility="Collapsed">
                        <StackPanel>
                            <TextBlock Text="选择麦克风"
                                       FontSize="22"
                                       FontWeight="Black"
                                       Foreground="{DynamicResource TextPrimary}"/>
                            <TextBlock Text="说话时音量条有跳动，就说明麦克风基础配置正常。"
                                       Margin="0,8,0,20"
                                       FontSize="14"
                                       Foreground="{DynamicResource TextSecondary}"/>
                            <ComboBox x:Name="MicComboBox"
                                      Height="38"
                                      DisplayMemberPath="Name"
                                      SelectionChanged="MicComboBox_SelectionChanged"
                                      Margin="0,0,0,24"/>
                            <Border Background="{DynamicResource ControlBackground}"
                                    BorderBrush="{DynamicResource BorderDefault}"
                                    BorderThickness="1"
                                    CornerRadius="8"
                                    Padding="18">
                                <StackPanel>
                                    <TextBlock Text="实时音量"
                                               FontSize="16"
                                               FontWeight="Bold"
                                               Foreground="{DynamicResource TextPrimary}"
                                               Margin="0,0,0,12"/>
                                    <ProgressBar x:Name="MicLevelBar"
                                                 Height="18"
                                                 Minimum="0"
                                                 Maximum="100"
                                                 Foreground="{DynamicResource AccentBlue}"
                                                 Background="{DynamicResource BorderStrong}"/>
                                    <TextBlock x:Name="MicStatusText"
                                               Text="正在准备麦克风..."
                                               Margin="0,12,0,0"
                                               FontSize="13"
                                               Foreground="{DynamicResource TextSecondary}"/>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </Grid>

                    <Grid x:Name="ScannerPage" Visibility="Collapsed">
                        <StackPanel>
                            <TextBlock Text="扫码枪（可选）"
                                       FontSize="22"
                                       FontWeight="Black"
                                       Foreground="{DynamicResource TextPrimary}"/>
                            <TextBlock x:Name="ScannerIntroText"
                                       Text="没有扫码枪可直接进入下一步；如需使用，可在这里测试扫码枪"
                                       Margin="0,8,0,18"
                                       FontSize="14"
                                       Foreground="{DynamicResource TextSecondary}"/>
                            <Border Background="{DynamicResource ControlBackground}"
                                    BorderBrush="{DynamicResource BorderDefault}"
                                    BorderThickness="1"
                                    CornerRadius="8"
                                    Padding="10"
                                    Margin="0,0,0,12">
                                <StackPanel HorizontalAlignment="Center">
                                    <Image x:Name="TestBarcodeImage"
                                           Width="430"
                                           Height="88"
                                           Stretch="Uniform"/>
                                    <TextBlock x:Name="TestBarcodeText"
                                               Margin="0,6,0,0"
                                               FontSize="12"
                                               HorizontalAlignment="Center"
                                               Foreground="{DynamicResource TextSecondary}"/>
                                </StackPanel>
                            </Border>
                            <TextBox x:Name="ScanTestTextBox"
                                     Height="38"
                                     FontSize="16"
                                     Padding="10,0"
                                     VerticalContentAlignment="Center"
                                     PreviewKeyDown="ScanTestTextBox_PreviewKeyDown"
                                     TextChanged="ScanTestTextBox_TextChanged"/>
                            <Border Margin="0,12,0,0"
                                    Background="{DynamicResource ControlBackground}"
                                    BorderBrush="{DynamicResource BorderDefault}"
                                    BorderThickness="1"
                                    CornerRadius="8"
                                    MinHeight="138"
                                    Padding="14">
                                <TextBlock x:Name="ScanStatusText"
                                           Text="没有扫码枪可直接进入下一步"
                                           FontSize="13"
                                           LineHeight="20"
                                           TextWrapping="Wrap"
                                           Foreground="{DynamicResource TextSecondary}"/>
                            </Border>
                        </StackPanel>
                    </Grid>

                    <Grid x:Name="DonePage" Visibility="Collapsed">
                        <StackPanel>
                            <TextBlock Text="配置完成"
                                       FontSize="22"
                                       FontWeight="Black"
                                       Foreground="{DynamicResource TextPrimary}"/>
                            <TextBlock Text="下面是日常打包流程。"
                                       Margin="0,8,0,20"
                                       FontSize="14"
                                       Foreground="{DynamicResource TextSecondary}"/>
                            <Border Background="{DynamicResource ControlBackground}"
                                    BorderBrush="{DynamicResource BorderDefault}"
                                    BorderThickness="1"
                                    CornerRadius="8"
                                    Padding="18">
                                <TextBlock x:Name="FlowText"
                                           FontSize="16"
                                           LineHeight="30"
                                           TextWrapping="Wrap"
                                           Foreground="{DynamicResource TextPrimary}"/>
                            </Border>
                            <Border Margin="0,14,0,0"
                                    Background="{DynamicResource ControlBackground}"
                                    BorderBrush="{DynamicResource BorderDefault}"
                                    BorderThickness="1"
                                    CornerRadius="8"
                                    Padding="16">
                                <StackPanel>
                                    <TextBlock Text="局域网查看视频"
                                               FontSize="15"
                                               FontWeight="Bold"
                                               Foreground="{DynamicResource TextPrimary}"/>
                                    <TextBlock Text="完成向导后会显示手机连接二维码。手机与这台电脑连接同一个局域网，扫码即可打开打包视频和回放页面。"
                                               Margin="0,7,0,0"
                                               FontSize="13"
                                               LineHeight="20"
                                               TextWrapping="Wrap"
                                               Foreground="{DynamicResource TextSecondary}"/>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </Grid>
                </Grid>
            </Border>
        </Grid>

        <Border Grid.Row="2"
                Margin="0,22,0,0"
                BorderBrush="{DynamicResource BorderLight}"
                BorderThickness="0,1,0,0"
                Padding="0,16,0,0">
            <Grid>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                    <Button x:Name="SkipButton"
                            Content="跳过"
                            Style="{StaticResource SecondaryButtonStyle}"
                            Click="SkipButton_Click"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="BackButton"
                            Content="上一步"
                            Style="{StaticResource SecondaryButtonStyle}"
                            Margin="0,0,10,0"
                            Click="BackButton_Click"/>
                    <Button x:Name="NextButton"
                            Content="下一步"
                            Style="{StaticResource PrimaryButtonStyle}"
                            Click="NextButton_Click"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
