<Window x:Class="ExpressPackingMonitoring.UI.CameraBarcodeUpgradeDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="新增摄像头识别面单"
        SizeToContent="Height"
        Width="520"
        WindowStyle="ToolWindow"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource PanelBackground}"
        Foreground="{DynamicResource TextPrimary}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="无需扫码枪，也能自动开始录像"
                       FontSize="18"
                       FontWeight="SemiBold"
                       Foreground="{DynamicResource TextPrimary}"/>
            <TextBlock Text="将面单条形码放入预览引导框，稳定识别后会自动开始或切换录像"
                       Margin="0,12,0,0"
                       FontSize="14"
                       LineHeight="22"
                       TextWrapping="Wrap"
                       Foreground="{DynamicResource TextSecondary}"/>
            <Border Margin="0,16,0,0"
                    Padding="14"
                    CornerRadius="8"
                    Background="{DynamicResource ControlBackground}"
                    BorderBrush="{DynamicResource BorderLight}"
                    BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="扫码枪仍可同时使用，建议保留作为后备"
                               FontSize="14"
                               FontWeight="SemiBold"
                               TextWrapping="Wrap"
                               Foreground="{DynamicResource TextPrimary}"/>
                    <TextBlock Text="支持常见快递面单的一维条码，商品条码不会触发录像"
                               Margin="0,6,0,0"
                               FontSize="13"
                               TextWrapping="Wrap"
                               Foreground="{DynamicResource TextSecondary}"/>
                </StackPanel>
            </Border>
        </StackPanel>

        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,22,0,0">
            <Button Content="启用摄像头识别"
                    Style="{StaticResource PrimaryButtonStyle}"
                    Margin="0,0,10,0"
                    Click="Enable_Click"/>
            <Button Content="暂不启用"
                    Style="{StaticResource SecondaryButtonStyle}"
                    Click="NotNow_Click"/>
        </StackPanel>
    </Grid>
</Window>
